{
  "name": "brownsville-bwt-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for CBP Brownsville wait-times RSS",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
